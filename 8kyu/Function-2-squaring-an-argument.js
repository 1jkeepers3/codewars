// Now you have to write a function that takes an argument and returns the square of it.

//My solution

// Write the "square"-function here
function square(arg){
    //find square of arg
    // return it
    return Math.pow(arg, 2)
  }
  
  //PREP
  //P: argument, num, + or -
  //R: return square of the argument
  //E:squareRoot(11) -> 121, squareRoot(10)->100, squareRoot(11.8321)->140
  //P: